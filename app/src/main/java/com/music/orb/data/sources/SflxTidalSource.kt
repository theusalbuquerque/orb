package com.music.orb.data.sources

/**
 * Legacy Orb source removed from the BitChord v1.5 playback pipeline.
 *
 * This intentionally contains no implementation. It is kept as a tombstone so
 * extracting the transplanted src over an older Orb checkout overwrites the old
 * source file instead of leaving stale TIDAL/SFLX code to be compiled.
 */
